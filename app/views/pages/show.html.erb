<p id="notice"><%= notice %></p></br></br></br></br></br></br></br></br></br>

<div id="comment">
<div id="new">
<h2>Add a comment:</h2>
<%= form_for([@page, @page.comments.build], :remote => true, :html => { :id => 'comment_form' }) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :rows=>'3' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<p>
  <b></b>
  <%= @page.title %>
</p>


<p>
  <b>Body:</b>
  <%= @page.body %>
</p>

<hr/>
<div id="com">
<h2>Comments:</h2>
<div id="p_comments">
<%= render (@page.comments) %>
</div>
</div>
<hr/>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
</div></div>
